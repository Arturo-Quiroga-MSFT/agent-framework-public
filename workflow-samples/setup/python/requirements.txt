# Requirements for Python agent creation script

# Azure AI Projects SDK (for agent creation)
azure-ai-projects>=1.0.0b1

# Azure Identity (for authentication)
azure-identity>=1.16.0

# YAML parsing
pyyaml>=6.0.1
