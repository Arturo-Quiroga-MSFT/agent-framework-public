# NL2SQL Pipeline Environment Configuration
# Copy this file to .env and fill in your values

# ==== REQUIRED: Azure OpenAI Configuration ====
# Get these from Azure Portal → Azure OpenAI Service
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4

# ==== REQUIRED: MSSQL Connection (Choose ONE method) ====

# Method A: Use existing connection ID (from previous connection)
# Recommended if you've already connected
#MSSQL_CONNECTION_ID=your-connection-id-uuid

# Method B: Auto-connect with server details
# Workflow will connect automatically on startup
MSSQL_SERVER_NAME=your-server.database.windows.net
MSSQL_DATABASE_NAME=your-database-name

# ==== OPTIONAL: Advanced MSSQL Settings ====
# Uncomment if you need to customize connection behavior
#MSSQL_AUTHENTICATION_TYPE=ActiveDirectoryDefault
#MSSQL_USERNAME=your-username
#MSSQL_PASSWORD=your-password

# ==== OPTIONAL: Security Settings ====
# Control query safety and limits
#MAX_ROWS=1000                    # Maximum rows per query (default: 1000)
#ALLOW_WRITE_OPERATIONS=false     # Enable INSERT/UPDATE/DELETE (default: false)
#QUERY_TIMEOUT_SECONDS=30         # Query timeout (default: 30)

# ==== OPTIONAL: Observability & Tracing ====
# Choose ONE tracing method (or none for no tracing)

# Option 1: Console Tracing (Simplest - outputs to terminal)
#ENABLE_CONSOLE_TRACING=true

# Option 2: OTLP Endpoint (For Jaeger, Zipkin, etc.)
#OTLP_ENDPOINT=http://localhost:4317

# Option 3: Application Insights (Azure)
#APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=xxx;IngestionEndpoint=xxx

# Option 4: DevUI Tracing (Visual debugging in web interface)
#ENABLE_DEVUI_TRACING=true

# ==== OPTIONAL: Azure AI Project (Alternative to OpenAI) ====
# Use these if you have an Azure AI Foundry project
#AZURE_AI_PROJECT_ENDPOINT=https://your-project.services.ai.azure.com/api/projects/yourProject
#AZURE_AI_MODEL_DEPLOYMENT_NAME=gpt-4

# ==== OPTIONAL: Development Settings ====
# Uncomment for development mode
#DEBUG=true
#LOG_LEVEL=DEBUG
#ENABLE_QUERY_LOGGING=true
#CACHE_SCHEMA=true
#SCHEMA_CACHE_TTL_SECONDS=300

# ==== OPTIONAL: Production Settings ====
# Uncomment for production deployment
#ENVIRONMENT=production
#LOG_LEVEL=INFO
#ENABLE_AUDIT_LOGGING=true
#MAX_CONCURRENT_QUERIES=10
#ENABLE_QUERY_QUEUE=true

# ==== NOTES ====
# 1. Never commit the actual .env file to version control
# 2. Ensure Azure SQL firewall allows your IP address
# 3. Run 'az login' before starting the workflow
# 4. Default port for DevUI is 8097 (configurable in code)
# 5. For security, use Managed Identity in production instead of connection strings
